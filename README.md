# Airboat Navigator MVP

Веб-приложение для демонстрации цифрового штурмана аэролодки. Просчет маршрута выполняется отдельным C++ движком, веб-часть только показывает данные и вызывает расчет.

## Запуск

```bash
npm run build
npm start
```

После запуска открыть:

```text
http://localhost:5173
```

## Demo-датчики и текущая позиция судна

Для имитации бортовых датчиков запустить отдельный локальный simulator:

```bash
npm run sensor:demo
```

Во втором терминале запустить основной интерфейс:

```bash
npm start
```

Основной backend отдает состояние судна через:

```text
GET /api/v1/sensor-state?mode=demo|real|off
```

В интерфейсе есть переключатель `Источник датчиков`:

- `Demo simulator` — читает локальный simulator на `8081`;
- `Real CAN/NMEA` — читает будущий реальный adapter;
- `Отключены` — не опрашивает датчики, скрывает маркер судна и оставляет ручной расчет маршрута.

По умолчанию `server.js` читает demo-источник:

```text
http://127.0.0.1:8081/api/v1/sensor-state
```

Настройки simulator:

```bash
PORT=8081 TICK_MS=500 SPEED_MPS=8.5 npm run sensor:demo
```

Для реального Raspberry Pi/CAN режима frontend менять не нужно: достаточно поднять локальный adapter с тем же JSON-контрактом и указать:

```bash
REAL_SENSOR_STATE_URL=http://127.0.0.1:<adapter-port>/api/v1/sensor-state npm start
```

Дополнительно доступны:

```bash
SENSOR_STATE_MODE=off npm start
DEMO_SENSOR_STATE_URL=http://127.0.0.1:8081/api/v1/sensor-state npm start
SENSOR_STATE_TIMEOUT_MS=900 npm start
```

## Запуск в Docker

```bash
npm run docker:build
npm run docker:run
```

Контейнер слушает `0.0.0.0:5173`, снаружи приложение открывается на:

```text
http://localhost:5173
```

Через Docker Compose:

```bash
docker compose up --build
```

Compose поднимает два сервиса: основной интерфейс на `http://localhost:5173` и demo simulator на `http://localhost:8081/api/v1/sensor-state`. Остановить:

```bash
docker compose down
```

Если порт занят:

```bash
APP_PORT=5174 SENSOR_PORT=8082 docker compose up --build
```

## Что внутри

| Путь | Назначение |
| --- | --- |
| `cpp/route_engine.cpp` | C++ расчетный движок |
| `scripts/import_pipisa.js` | импорт пользовательского JSON в формат движка |
| `scripts/demo_sensor_simulator.js` | локальная имитация NMEA/CAN sensor-state |
| `server.js` | локальный web/API сервер |
| `web/` | интерфейс |
| `app_data/` | нормализованные данные приложения |
| `data/` | исследовательские и seed-данные MVP |
| `cpp/nmea/` | заготовка будущего CAN/NMEA adapter |
| `system/real_map_mvp_implementation.md` | описание реализации реальной карты и расчетных артефактов |
| `docs/nmea_sensor_state.md` | контракт Sensor State API и схема demo/real интеграции |
| `docs/tz_status_and_roadmap.md` | сверка с ТЗ, список фич и план доработки |
| `docs/formuly_aerolodki.pdf` | описание расчетных формул в PDF |

## Модель расчета

Движок строит маршрут по графу из JSON:

- исключает сложные поверхности без поддува;
- считает скорость участка с учетом выбранного режима, узких водных линий, поверхности и возможности глиссирования;
- считает сопротивление, требуемую мощность, расход топлива через BSFC и запас относительно резерва;
- считает риск по поверхности, скорости и узости участка;
- выбирает стоимость по режиму: быстрый, экономичный, кратчайший, безопасный.

Данные синтетические и не предназначены для реальной навигации.

## Реальная карта

Интерфейс показывает маршрут поверх нейтральной светлой подложки без картографических подписей через Leaflet. Расчетный граф строится автоматически поверх зоны Красноярска, линейных OSM-waterway и локальных поверхностных зон:

- треугольный navmesh по карте: шаг около `0.12 км`;
- водные линии OSM уплотняются узлами через `0.08 км`;
- водные линии соединяются между собой и с navmesh, чтобы маршрут проходил по узким протокам;
- пользовательские тестовые ребра из JSON сохранены как reference, но не используются как runtime-граф.

Файл геопривязанного графа:

```text
app_data/route_graph_georef.geojson
```

Подложка карты требует интернет для загрузки CARTO/OSM-тайлов без подписей, но расчет маршрута выполняется локально через C++ бинарник.

## Постановка точек на карте

В интерфейсе можно выбрать точку из списка, написать название вручную или указать координаты в формате `55.99, 92.86`. Маршрут поддерживает до 5 точек: старт, 3 промежуточные точки и финиш. Сервер считает каждое плечо через C++ движок и склеивает участки в один результат.

В каталог включены: `Дивногорск`, `Бирюса`, `КрасГЭС`, `Усть-Мана`, `Красноярск`, `Остров Отдыха`, `Предмостная площадь`, `Остров Татышев`, `Стрелка`, `Абаканская протока`, `Пашенный`, `Академгородок`, `Базаиха`, `Торгашино`, `Николаевка`. Также есть кнопки:

- `Поставить старт на карте`;
- `Поставить финиш на карте`.

После клика по карте приложение:

1. ставит пользовательскую точку;
2. находит ближайший узел расчетного графа;
3. показывает линию привязки или пеший добор от воды до точки;
4. пересчитывает маршрут через C++ движок.

Если до выбранной точки нет полного водного пути, сервер строит маршрут до ближайшей достижимой воды и добавляет пунктирный пеший участок до настоящего финиша.

Все возможные пути графа постоянно видны на карте и окрашены по типам поверхностей: вода, лед, мелководье, трава/камыш, шуга/наледь, камни, болото.

## Автоматический граф

Рабочий маршрутный граф больше не берется из тестовых ребер JSON. Импортёр делает так:

1. строит или читает зоны поверхностей;
2. сохраняет их в `app_data/surface_zones.geojson`;
3. разбивает карту на сетку;
4. классифицирует узлы и ребра по зонам;
5. генерирует `app_data/route_graph_georef.geojson`;
6. передает C++ движку уже автоматически построенный граф.

Текущая генерация создает `26088` узлов и `57624` ребра. Ребра `map.edges` из пользовательского JSON сохранены только как тестовый reference.

## Сверка с ТЗ

Краткая сверка с постановкой, список реализованных фич, пробелы и план доработки находятся в:

```text
docs/tz_status_and_roadmap.md
```
